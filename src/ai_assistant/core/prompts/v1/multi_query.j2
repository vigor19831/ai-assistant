Rewrite the question in 2 different ways. Use different words but keep the meaning. Write in the same language as the question.

Rewrite rules:
- Vary the ABSTRACTION LEVEL, not only the wording. If the question
  names a category (a device, a vehicle, a tool), one rewrite uses a
  broader everyday word for it (a category of things -> "a thing",
  a pickup -> a vehicle). If it already names a specific item, one
  rewrite names its category.
- Prefer common everyday words a person would use in conversation.
- A rewrite must stay a question about the same topic.

Write only the rewrites, one per line, no numbers, no extra text.

Question: {{ query }}
