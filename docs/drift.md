# Known Architectural Drift

| ID | File | Broken Rule | Why | Fix | Priority |
|----|------|-------------|-----|-----|----------|
| 1 | Fixed 2026-06-09: get_context_limit() added to ILLM port. All adapters updated. |
| 2 | Fixed 2026-06-09: NullReranker introduced. `rerank()` no longer branches on `None`. `InitializedAppState.reranker` is `IReranker` (non-optional). |
| 3 | Fixed 2026-06-09: getattr(config, "vector_store") removed. Pydantic validation guarantees field presence. |
| 4 | Fixed 2026-06-13: Replaced all `getattr(config, "x", default)` with direct `config.x` access in all adapters. All defaults verified in `AppConfig` Pydantic models. |
| 5 | Fixed 2026-06-14: `ChunkMetadata` schema drift. `vector_store_faiss.py` and `vector_store_memory.py` serialized `created_at` (not in domain model) and missed `total_chunks` in FAISS `add()`. Fixed via local `_chunk_to_dict`/`_chunk_from_dict` helpers in each adapter for strict deserialization matching domain model. |
| 6 | Fixed 2026-06-14: Added `get_logger` to `embedder_openai_compatible.py` and `reranker_api.py`. All `AdapterError` wraps now preceded by `logger.exception`. |
| 7 | `adapters/embedder_openai_compatible.py`, `adapters/llm_openai_compatible.py` | Duplicate HTTP client setup (httpx.AsyncClient, POST, raise_for_status, json parse) | Layer boundaries prevent shared httpx code: core/ is stdlib-only, adapters/ may only import core/*. Extracting to core/ violates stdlib-only rule. Extracting to adapters/_shared violates layer boundaries. | Accept duplication as architectural constraint. Revisit if >3 adapters share pattern. | Low |
| 8 | Fixed 2026-06-18: Replaced `metadata: dict[str, Any]` with explicit typed fields in `PipelineData` (embedder, vector_store, reranker, llm, pipeline_config, query_embedding, tokenizer_model, rerank_filtered_out, rerank_scores). All casts and string-key access removed from pipeline steps and tests. |
| 9 | ~~`src/ai_assistant/core/pipeline_steps.py`~~ (fixed 2026-06-17) | ~~`model: str = "gpt-4o"` default~~ | ~~Default был fallback для тестов/CLI~~ | **Fixed:** убраны все дефолты `"gpt-4o"` из `_estimate_tokens()` и `_truncate_to_fit()`. `tokenizer_model` теперь обязательный ключ в metadata. `ChatManager` всегда передаёт его. Все тесты обновлены. | — |
| 10 | `src/ai_assistant/core/retry.py` | `max_retries=3, delay=1.0, backoff=2.0` хардкод в `@with_retry` на адаптерах | Resilience policy — не бизнес-логика. Меняется раз в 10 лет. Вынесение в config требует CORE CHANGE (новые поля в `EmbedderConfigData`, `LLMConfigData`, `RerankerConfigData`) + inline-фабрики декораторов (костыль). | Accept as architectural constraint. Пересмотреть при добавлении `IRetryPolicy` порта или embedded-режима с 0 retries. | Low |
| 11 | `src/ai_assistant/core/prompts/__init__.py` | Jinja2 import в core/ | Prompt rendering — domain logic, Jinja2 — implementation detail. Для 30-летнего горизонта абстракция `IPromptRenderer` предпочтительна, но требует нового порта + адаптера + обновления factory + всех вызовов. | Accept as grandfathered exception. Документировать при добавлении второго движка шаблонов (Mustache, etc.). | Low |
| 12 | `src/ai_assistant/core/prompts/__init__.py` | `_make_hashable()` без защиты от циклических ссылок | Текущие prompts не содержат self-referencing dataclasses. Защита — YAGNI. | Accept. Добавить при первом `RecursionError` в продакшене. | Low |
| 13 | `core/domain/documents.py` | `original_path` smuggled through pipeline without contract | No first-class field for source URI propagation | Added `source_uri` to `ChunkMetadata` (CORE CHANGE) | Low |
| 14 | `src/ai_assistant/api/deps.py` (`RAGState._status: dict[str, dict[str, object]]`) | No `Any`/`object` where concrete type is visible (AI Rules Section 9, TYPES). | Partially fixed 2026-06-20: changed from `dict[str, object]` to `dict[str, dict[str, object]]`. Inner values are still `object` (timestamps float, strings, nested dicts). Full fix requires CORE CHANGE. | Introduce `ReindexStatusEntry` dataclass in `core/domain/` and type `RAGState._status` as `dict[str, ReindexStatusEntry]`. Requires updating `handlers.py`, `test_rag.py`, `test_contracts.py`. **Trigger: when RAGState._status gains a 5th distinct field type or when background task status is exposed via API.** | Low |
| 15 | `src/ai_assistant/core/pipeline.py` (`_required_fields_for_steps`) | Upfront validation should cover all inputs; `query_embedding` is produced by previous step, not input. | Fixed 2026-06-18: `query_embedding` removed from `retrieve` required fields. Upfront validation no longer rejects valid `[embed_query, retrieve]` pipelines. Trade-off accepted: `[retrieve]` alone will fail at runtime with `QUERY_EMBEDDING_MISSING`, not at startup. | Introduce step dependency graph in `_required_fields_for_steps` to distinguish inputs from produced fields if more producer-consumer steps are added. **Trigger: when adding a pipeline step that produces a field consumed by a later step.** | Low |
| 16 | `src/ai_assistant/core/config.py` | Admin endpoints unprotected by default | Any client with api_key could mutate runtime security policy | Added `admin_enabled: bool = False` to `SecurityConfig`; admin endpoints return 404 unless explicitly enabled | Low |
| 17 | `src/ai_assistant/core/ports/vector_store.py`, `adapters/vector_store_faiss.py`, `adapters/vector_store_memory.py` | `delete()` required manual `save()` — caller could forget, causing lost updates on crash | Solo-maintained project: contract must enforce correctness, not discipline. Caller-side save() was forgotten in `reindex_documents()` clear path. | `delete()` now auto-persists with rollback on failure. Manual `save()` after `delete()` is redundant but harmless. **Trigger: when adding a 3rd vector store adapter or when bulk-delete optimization needs no-persist path.** | Low |
| 18 | `src/ai_assistant/api/security.py` | `_override_api_key` is process-local | Runtime API key rotation via `/admin/api-key` uses `threading.Lock()` which does not propagate across uvicorn/gunicorn workers. Each worker has its own copy of `_override_api_key`. | Documented in code comments and lifespan warning. Use `AI_SECURITY_API_KEY` env var for multiprocess deployments. | Low |
| 19 | `src/ai_assistant/core/config.py` | Pydantic + PyYAML import в core/ | `AppConfig` требует сложной вложенной валидации, парсинга env-переменных с префиксами и backward-compatible миграций. Реализация на stdlib `dataclasses` + ручная валидация добавит ~500+ строк и постоянную поддержку. Pydantic `BaseSettings` даёт критичный для деплоя env-prefix `AI__`. PyYAML необходим для загрузки config-файлов. | Accept as grandfathered exception. Вынесение в `adapters/` потребует порта `IConfigLoader`, ломая простой `load_config()` API, используемый в scripts, api, features. Пересмотреть если Pydantic станет unmaintained. | Low |
| 20 | `src/ai_assistant/core/utils.py` | tiktoken + tokenizers import в core/ | Точный подсчёт токенов требует BPE-токенизации (tiktoken для OpenAI, tokenizers для HuggingFace). Переimplementation в stdlib — тысячи строк, подвержен ошибкам, требует vocab-файлы. Код уже имеет graceful fallback (`len(text)//4` / `len(text)`) при отсутствии библиотек. | Accept as grandfathered exception. `count_tokens()` — утилита, не достойная отдельного порта. Пересмотреть при добавлении локального tokenizer-порта или переходе на чисто character-based estimation. | Low |

Rule: Do not add new drift if old pattern can be fixed properly.
