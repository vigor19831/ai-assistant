# Known Architectural Drift

> Rule: Do not add new drift if old pattern can be fixed properly.
> AI reads this file before any architectural output (Document Meta §12).
> ACTIVE entries are constraints — do not "fix" them without explicit user request.
> FUTURE RISKS are deferred issues with concrete triggers — they are known, not forgotten.
> Git history is unreliable (commits often say "fix"). This file is the source of truth.

## ACTIVE (6)

| ID | Since | Location | Constraint | Exit Criteria |
|----|-------|----------|------------|---------------|
| 11 | 2026-06-09 | `core/prompts/__init__.py` | Jinja2 import in stdlib-only `core/` layer | Second template engine needed OR Jinja2 deprecated |
| 18 | 2026-06-14 | `api/security.py` | `_override_api_key` process-local; does not propagate across uvicorn/gunicorn workers | Multiprocess deployment becomes primary use case |
| 19 | 2026-06-14 | `core/config.py` | Pydantic + PyYAML import in stdlib-only `core/` layer. `config_version` + backward-compat loader in place | Pydantic 2y without release OR critical CVE unpatched >6mo |
| 29 | 2026-07-05 | `core/ports/tokenizer.py` | `ITokenizer` simplified; no multi-encoding support | Multi-encoding support needed |
| 34 | 2026-07-08 | `tests/test_stateful_ports.py` | `asyncio.run()` in `ThreadPoolExecutor` for Hypothesis (issue #4107 — no native async) | Hypothesis adds async state machine OR tests removed |
| 38 | 2026-08-12 | `tests/conftest.py` | `AsyncMock` cannot mock async generators → `MagicMock(side_effect=factory)` bypasses `spec=ILLM` | stdlib native async generator mock support OR `ILLM.stream` contract change |

## FIXED → Rule Extracted (see docs, no details needed)

| ID | Fixed | Rule / Location |
|----|-------|-----------------|
| 23 | 2026-06-29 | HTTP client ownership → `architecture.md` §4, §5 |
| 22 | 2026-06-28 | Port objects own config, PipelineData carries references → `architecture.md` §8, `ai_rules.md` §2.2 |
| 7 | 2026-06-28 | Shared CODE ok, shared RESOURCE banned → `architecture.md` §4.3, §8 |
| 14 | 2026-06-26 | Untyped `dict[str, dict]` bags banned → `architecture.md` §9 (antipatterns) |
| 8 | 2026-06-18 | `PipelineData.metadata: dict[str, Any]` replaced with typed fields → `architecture.md` §9 (antipatterns) |
| 31 | 2026-07-10 | Unconditional `shutdown()`, no `_closed` flag → `architecture.md` §6 |
| 30 | 2026-07-10 | `SystemMessage` in domain, removed from `ILLM` port → `architecture.md` §8 |
| 37 | 2026-07-28 | `min_relevance_score` stripped; strict rank-only → `architecture.md` §13 |
| 28 | 2026-08-19 | Removed `require_api_key` duplication from `admin.py` → `api/admin.py` |
| 39 | 2026-08-19 | Moved `_build_fallback_prompt` to `prompts/v1/fallback.j2` → `core/pipeline_steps.py` |
| 36 | 2026-07-19 | `threshold` removed; rank-only invariant. No deprecation cycle (pre-production, solo, no legacy configs) → `architecture.md` §13 |
| 35 | 2026-07-10 | `chat_history: tuple[tuple[str, str], ...]` eliminates runtime type introspection |
| 19+25 | 2026-06→07 | Config schema evolves via backward-compat loaders + `config_version` → `core/config.py` pattern |

## FIXED → History (one-liners, self-contained)

Recent window 2026-08-27 → now (working horizon). Pre-August archive
(#1–#39): one-liners in git history of this file; every lesson with a
surviving contract is extracted into the Rule Extracted table above,
the rest were plain bugfixes.

| ID | Fixed | Summary |
|----|-------|---------|
| 40 | 2026-08-27 | Delete-all left stale index files → deleted chunks resurrected on restart; empty namespace now removes its files (both vector store adapters) |
| 41 | 2026-08-27 | Memory store `save()` overwrote skipped namespaces with an empty store on shutdown; never-loaded namespaces no longer touch disk |
| 42 | 2026-08-27 | Watcher consumed the snapshot on failure → partial index frozen as complete; bounded retry (3 attempts), snapshot consumed on success only |
| 43 | 2026-08-28 | Retry lives in exactly one layer → `ai_rules.md` §7, `architecture.md` §9 (antipatterns) |
| 44 | 2026-08-27 | Mid-stream failure persisted a truncated assistant turn; save now runs only after the stream completes |
| 45 | 2026-08-27 | SSE frames carried multi-line chunks under one `data:` prefix — strict clients dropped the Sources block; every line carries its own prefix |
| 46 | 2026-08-27 | BOM files decoded with `utf-8` first → U+FEFF leaked into chunk text; `utf-8-sig` first, plain `utf-8` removed |
| 47 | 2026-08-27 | `/rag/reindex` without sources → `UnboundLocalError` masked as 500; explicit 400 before spawning the task |
| 48 | 2026-08-27 | Memory store FIFO-evicted on max_chunks overflow (eviction banned by ai_rules §2; RAM pressure never measured) and persisted the loss; eviction removed, `add()` rejects like faiss — contract in port |
| 49 | 2026-08-28 | Sources block persisted into chat history — token waste, condense pollution, evidence feedback loop; history stores the clean answer, API keeps the block |
| 50 | 2026-08-28 | Refusal answers carried chunks as sources; strict-RAG refusal = empty sources/chunks_used=0; refusal strings in `core/constants.py` + prompt↔matcher sync test |
| 51 | 2026-08-28 | OAI chat history was write-only; handler now loads stored history on conversation_id without client messages (client-first) |
| 52 | 2026-08-31 | Query-path retry stacking removed (16 attempts worst case) — retry lives strictly in adapters; `PipelineConfig.retry`, `RetryConfig`, `retry_with_config` purged (CORE CHANGE) |
| 53 | 2026-08-31 | Dead LLM sampling fields purged (`top_k`, `min_p`, `repeat_penalty`, `presence_penalty`, `frequency_penalty`): never reached the adapter — config lied; no migration (#36 precedent) |
| 54 | 2026-08-31 | Defaults `temperature: 0.0`, `top_p: 1.0` — RAG determinism by default; killed 15/17-vs-17/17 sampling flakiness |
| 55 | 2026-08-31 | check_rag harness: run-unique conv-id, Sources stripped from client history, full answers logged on errors; §13.4-verified instrument fixes, chat e2e re-baselined |
| 56 | 2026-08-31 | check_rag: crash-blind error-* tests (500 passed "must not crash"); one-word negation guard; crash=FAIL, 3-word window |
| 57 | 2026-08-31 | Second dead-field batch purged (`n_batch`/`n_ubatch`/`mmap`/`mlock` × llm/embedder): read by neither adapters nor run_servers.py; knobs live in run_servers.yaml extra_args; `n_gpu_layers` kept (live). CORE CHANGE, no migration |
| 58 | 2026-08-31 | condense_question.j2 rewritten: zero-shot template → 7B meta-question drift; new contract (resolve pronouns; ask about the topic, never documents; output-only-question) + cross-domain example; both models re-baselined ×2 |
| 59 | 2026-09-01 | index_documents.py removed — third executor over one index (script + watcher + API): CPU contention, timeout cycles, disk/memory divergence; no unique function — watcher (60 s poll, 300 s window) + POST /rag/reindex + prepare_docs.py (large files) cover all cases |
| 60 | 2026-09-01 | Dual -ngl flags: run_servers.py built -ngl from config.yaml, run_servers.yaml extra_args appended it again; llama.cpp keeps the last flag → embedder ran "-ngl 0 ... -ngl 99", died at startup ("HTTP request failed", 47 ms); all 08-31 "GPU indexing" runs were conflicted-flag. Rule: n_gpu_layers lives in config.yaml only, extra_args never carries -ngl |
| 61 | 2026-09-01 | Reranker env-block removed (double CPU-gating) + reranker.n_gpu_layers in schema (RerankerConfig/Data/mapper, pattern #57 — extra="forbid" caught the missing field at startup, as designed). First live GPU indexing: 3400 chunks — CPU never completed (300 s window, ~4 chunks/s); GPU ~2 min at LLM=10 layers, 2.3/4 GB VRAM |
| 62 | 2026-09-01 | Concurrent reindex executors (manual task + watcher poll) over one embedder: watcher's 3-attempt cycle ran parallel to the task, timed out independently while the task completed (mtime clean, find -newermt empty). Rule: one reindex path at a time |
| 63 | 2026-09-01 | Vector store memory→faiss at live-corpus growth (user-declared 1–2K files/mo; ~40–60K chunks projected vs 100K trigger): RAM + disk (faiss binary ~×5 smaller than JSON-float; 202 MB JSON = old crash-era layers, clean start = 59 KB/doc). Provider switch in config; full reindex = the migration (faiss reads no JSON stores) |
| 64 | 2026-09-01 | SOURCE_INDEX_TIMEOUT (300 s) killed each pass at ~85% (2900/3397 chunks; ~10 chunks/s GPU at LLM=10) — progress RAM-only until the final store write, retries restarted from zero; embed.progress logging exposed it. Fixes: window 300→600 s (measured ×1.7); document-level checkpoints in index_folder (embed→upsert→atomic-save; kill loses ≤1 doc; resume via filter_unchanged_docs) — watcher window = pause between checkpoints, never a reset. GPU rate ~10 chunks/s → 5.5 min/MB. VERIFIED 2026-09-02: kill-midway invariants green (initial red = wrong test expectation, chunks-per-doc > 1 — a replica proved the pipeline correct); benchmark 17/17 + 25/30 on faiss with live reindex of the 3397-chunk corpus |
| 65 | 2026-09-02 | Atoms pipeline: 142 KB → 12 parts × 12 KB → 21m16s, zero HTTP errors. Archivist profile in config.yaml (part_bytes 12000 = ctx 8192 − 800 instr − 1500 answer; llm_model null → field omitted, None-guard vs str(None)="None"). Four 400s root-caused: model-name mismatch → omit field; 30 KB parts → 11963 tokens → exceed_context_size → token-budget split; None-string → guard. mtime idempotency (per-layer SKIP). Default: split only — atoms explicit just-in-time on mature chats. Verification: 3/3 control questions as synthesis, atoms layer in Sources; rerank top_score 0.007–0.12 on consolidation queries (an atom answers one question — "list all decisions" matches no single chunk by construction); synthesis judged by answer shape + layer coverage, not rerank score |
| 66 | 2026-09-03 | prepare_docs: freshness marker checked part01 only → small as-is files reprocessed every run (menu [2]: ~5 min → 127 ms SKIP); grown-past-threshold source kept stale as-is copy beside new parts → both indexed (fix: `_reconcile_outputs` in split_file + main); atom seam //2 → //4 (17041 B on 12000 B budget, measured). Rule: idempotency marker = the exact artifact the producer writes; freshness tests drive the real producer, not hand-crafted outputs. Tests 17→20 |
| 67 | 2026-09-03 | Archivist A/B (watch corpus, same split): 7B crown — RU atoms 2.5/3; 4B rejected for RU (0.5/3: EN atoms, EN answers on RU queries), ×3 speed + clean facts (no caliber≈watch merge) as reserve. THE DECISION TEST: no user quote → not a decision; 4/4 "No user decisions" blocks, Q5-class answers stopped asserting a user choice. Cross-model proof: raw corpus reproduces false "you chose X" on BOTH models — atoms = status guard, a corpus property. Residual 7B ceilings: section duplication, year hallucination, EN on URL-heavy parts. 4B archivist viable only with per-part language header — see FUTURE RISKS |
| 68 | 2026-09-03 | Config window math as method (config.yaml never committed): window = context budget + generation + margin; max_context_tokens ~60% of window or long dialogs truncate silently. Rebalance 5000/10/0.15 → 4800/6/0.10, check_rag verified: 7B 17/17+8/9, 4B 17/17+9/9; cost: format-strict-1 → known limitation (0.10 margin trims a chunk on open synthesis). Rule: every rebalance = check_rag pass + a named cost |
| 69 | 2026-09-03 | check_all AST audit false positives: cycles now load-edges-only (flagged deps↔manager cycle was its own standard cure — function-scoped imports, layer-legal both ways); tests+scripts count as callers; stem fallback removed (marked same-named modules imported — false positive). Rule: grep-verify imports before orphan conclusions (registry keys "src." prefix vs real "ai_assistant.*" — grep-disproved). Recorded cause wrong: collector always visited function bodies |
| 70 | 2026-09-04 | scripts/ + launchers under ruff+mypy strict (84+66 first enable; RUFF_TARGETS/MYPY_TARGETS in check_all; check_rag per-file exempt per §13.4). First full [5] caught check_llm broken 4 days since #53/#57 — dead fields still passed to ConfigData. Audit finds: dead chat_query headers removed (auth on AsyncClient), decorative api_key stripped, stream errors printed (#55), TypedDict audit registry. Lesson: isolated mypy on scripts = false zero; src in target list always |
| 71 | 2026-09-04 | tests/ under ruff (§15 per §14): debt 372→0 — dead imports/vars, 12 import-redefs (arithmetic 971+9=1000, no shadowed tests), misplaced mock binding, fixture return-bug, NameError masked as AdapterError (import restored), SLEEP marker renamed "sleep: intentional" (noqa collision, 11 mo invisible); ruff format adopted; RU/CJK = test data (noqa). Lessons: anchors from fresh extraction, never memory/render; region-extractor loop for k-line files |
| 72 | 2026-09-05 | tests/ under mypy (owner decision): ~310 fixed — 9 fakes promoted to port ABCs (duck-typed contract defect), AppConfig(**dict)→model_validate, CHAT_NS_PREFIX into __all__, MYPY_TEST_FLAGS (pyproject overrides dead on mypy 1.20.2); ~80 annotation cosmetics frozen by disabled codes — deliberate, revisit when tests become a package. Rules: structural edits via sed banned (4 gate-caught accidents); ast.parse per patch; full commands only; fakes need explicit __init__; tests-mypy target must include src/ (#70 mirror) |
| 73 | 2026-09-05 | Tokenizer UX (3 owner findings): model switch bricked startup on missing tokenizer.json → huggingface adapter degrades to CharFallback (loud warning + recovery instruction; #48 pattern). Fatal stay: package missing, empty path, corrupt file. Warning says "restart" (config read once). check_llm: tokenizer block + compact redesign (verdict lines only, logger clamp — adapter tracebacks on down servers buried output; detail in app log). char over cl100k: universal, conservative overcount. Stale raise-test → fallback contract (gate-caught, expected) |
| 74 | 2026-09-05 | clean_cache + live logs (owner memory): Linux unlink on open `*.log` — process wrote into a deleted inode, log never reappeared; remembered "skipped (locked)" was Windows FS behavior, absent on Linux. Fix: `_stack_running()` guard (pgrep llama-server/uvicorn), live `.log` = SKIP with hint, cleanup after stop. Rule: OS-dependent protection is no protection — behavioral guards live in the script |
| 75 | 2026-09-05 | check_rag 7B ×8-run attribution: canon `17/17+8/9+24/30` byte-identical ×3 (score arrays equal — pipeline deterministic). 12:14 outlier `9/9+26/30` = char-fallback tokenizer (deleted-files window before download+restart; CtxTok ratios match char counts: RU ×1.5, EN +8%); not reproducible ×5 with exact tokenizer — margin/ctx-window/chat-budget leave CtxTok untouched (frictionless at bench-scale). Rule: fix tokenizer mode before judging runs; single-run deviation = find the config delta first |
| 76 | 2026-09-05 | Config-liveness audit (owner: "do yaml fields control anything?"): CLEAN — 0 dead knobs; 7 mapper builders pass every field (post-#53/#57 wiring sound); external consumers OK (run_servers `n_gpu_layers`/`server_context_size`, prepare_docs `archivist.*`); `debug` alive (main.py:90). Method: reader-pattern grep (`cfg.field`), not word-count (logger.debug noise = false readers). Budget knobs frictionless at bench contexts (72–263 tok vs 4800) — await real-corpus loads |
| 77 | 2026-09-05 | First live A/B (5 own chats, 12 questions): atoms PROVEN — retrieval completeness up (categories→models, final recommendations surface), URL-noise gone. DECISION TEST FAILED: 7 assistant opinions labeled `Status: decision` (subject inversion — "я бы выбрал" → "user decided"); atoms turned weak hallucination into evidenced lie: Q9 regressed from honest "I don't know" to "Вы выбрали Sprinter" with the atom as proof. Root cause isolated by probe (manual status demotion → honest answers restored): the atom Status label, not retrieval/generation. Fix task: `ARCHIVIST_PROMPT` decision-test hardening (7B inverts speaker in RU dialogues). Manual atom edits = measurement probe only, never a process |
| 78 | 2026-09-06 | Decision-guard campaign closed at benchmark maxima: 17/17 + 9/9 (first ever, multi-turn-1 green ×3). Live eval see-saw the benchmark cannot see: rule 12 ± fact-examples oscillates on advice-as-answer (4 swings, both directions: watch-advice quoted as decision-answer; router-recommendation falsely refused with its atom in Sources). Stable floor: validator auto-demotions (zero manual edits), autodom/cream/F6722 traps, atom body attribution. Lesson: benchmark-green ≠ live-green on 7B; cures = 14B class or Golden Set, not a 5th prompt iteration. Validator ChatGPT-format-bound — next: DeepSeek probe |
| 79 | 2026-09-07 | qwen 3.5 4B re-validated under campaign prompts; owner decision: 4B daily driver, waiting for next generation. Atoms: best ever (RU, zero fabricated decisions, 9.5 min vs 7B's 23 — ×2.4). Answers: all 4 decision-traps honest on own-chat corpus (see-saw absent — boundary was model-width, not prompt; DeepSeek-corpus counterexample → #81), EN on RU queries persists (~50%, #67); RU-repeat («на русском») recovers content-identical RU answers (manual re-ask + ~10 s) — initially recorded as 6/6 cycles + owner-accepted permanent tax; corrected same session: 3 full cycles, 1 broke RAG discipline (→ #81), tax retracted — hypothesis with counterexample. 4B-forever is a waiting position, not dogma: revisit at next model generation (14B-class affordable VRAM — Ceiling Log trigger) or when RU-answers become critical. Hybrid (4B archivist / 7B chat) remains the checked fallback, one config swap away |
| 80 | 2026-09-07 | Multi-service probe (DeepSeek chat, Block 2): format-agnostic layers hold — atoms clean (RU, zero fabricated decisions, bash summary-only works), answers exact (3/3 factual + trap honest), RU-answers on RU-content (4B language issue = source-language following, not inability); validator silent on ####-less format (expected soft-degrade, decisions unprotected — confirmed). NEW defect class: hallucinated dates (3 different dates for one undated chat, none real; source had PART-markers, atoms lost them → dates inferred from kernel-version context). Fix candidates: prompt «unknown date — never invent» + validator date-grep. DeepSeek regex for `_USER_BLOCK_RE`: not viable (no user-block markers). #78's «form dominates rules» applies to date-inference too |
| 81 | 2026-09-07 | DeepSeek corpus, extended live eval (4B, beyond #80's probe): decision-trap FAILED — raw chunk «Выполняем настройку...» answered as «была выполнена команда» (the campaign's lie class; #80's probe had passed it); same questions held on ChatGPT-format corpus → validator coverage is load-bearing: on ####-less exports silence = decisions unprotected, materialized failure not soft-degrade. RU-repeat broke RAG discipline 1/3 cycles (lecturing — bash scripts absent from sources); governor-RU repeat added an unsourced conjecture; one RU-direct query false-refused with data present in raw + atoms (cause unattributed: retrieval vs filter — open). #80 date hole confirmed end-to-end: invented «2026-08-07» cited in answers. Net: 4B-forever + RU-repeat = hypothesis with counterexample, not a proven mechanism; hybrid = checked fallback; date-fix candidates in #80 |
| 82 | 2026-09-07 | check_rag wiped live 'default' each run ([d]→config→default = watcher's documents/ target): ~580 chunks lost per run, 477/580 restored, live doc leaked into bench Sources, user saw wrong answers during restore. Fix: bench_000 + prefix [000] + teardown in finally ("000" rejected by API 422 — names start with a letter). Rule: bench never clears watcher-mapped namespaces. Open: watcher restoration gap after external clear (103 chunks) |
| 83 | 2026-09-07 | 4 FUTURE tests contradicted their own corpus (both models failed identically — instrument, not models): missing-1 deleted (dup missing-2); noise-1 distractor was user diet data → nutrition doc → round 2: medications query (pasta/coffee still answer diet); isolation-1 → Docker; missing-ru-1 → piano mirror. FUTURE 30→29, re-baseline ×2. Rule: verify test premises against the corpus, not intent |
| 84 | 2026-09-07 | Atoms consumption tier (component A): two synthetic atom docs in bench namespaces bench_atoms / bench_atoms_undated (prepare_docs --atoms format, RU); 5 FUTURE tests — decision attribution (#77), recommendation mirror, dated fact, undated 'when' -> 'date not specified' + year blacklist (#80/#81), RU answer language; FUTURE 29->34, total 51. Result: 4/5 green both models; atoms-undated-1 red on BOTH, distinct stable modes — 4B evades (what-for-when), 7B confabulates (merges unrelated fact as temporal, word-for-word stable) -> systemic date-unknown gap, date fix has a verifier; atoms-date-1 answers EN on both (latin-script pull, #67). Canons: 4B 29/34 x2 byte-identical; 7B 30-31/34 (multi-turn-1 flake) |
| 85 | 2026-09-08 | Date-fix campaign closed by stop-rule: rag_strict rule 13 ('date not specified' for undated 'when') hit its target (atoms-undated-1 green both runs, correct RU shape) but the 'X is not specified' pattern is contagious on 4B — every attempt leaked into adjacent tests: att.1 -> trap-1 date echo (contract breach) + missing-ru-1 verbose refusal; att.2 (scoped rule + never-repeat-dates + counter-example) -> trap-1 fixed, trap-2 broke (options echoed inside 'not specified' sentence) — displacement: block one echo outlet, pattern surfaces in another. Fully reverted; atoms-undated-1 = known limitation (4B evades, 7B confabulates). Lesson: 'not-specified' inherently invites naming the absent thing = question echo — structurally opposed to rules 4/5 anti-echo on small models; cures = 14B class or generation-side phrase guard, not a 3rd prompt iteration (#78 mirror). Revert left file residue (stray "</rules>>", spell-fixed "ultramarine" breaking the example<->query byte-match) — trap-2 stayed red until byte-identical canon restored; lesson: manual prompt edits drift silently — sync test guards only refusal strings |
| 86 | 2026-09-08 | Atom validator merged into prepare_docs (--validate mode + birth-time auto-report in make_atoms; owner architecture decision: one file, no new script). V1-V5 = format-agnostic text contract (drift #81); V6 = documents/ vs .store.json completeness (warn-only, #82-open, no faiss/server). First live run exposed two instrument defects, both provable independent of results and regression-locked: V5 dominance must count template-stripped atom CONTENT (EN Context values inside RU atoms flipped the file's script — 11 false flags); V6 identity is the file STEM (metadata.source = the doc id from read_sources, no extension — posix-path matching made every document both orphan and missing). Live verdict stays red deliberately (no exit-code coupling): 67 invented dates (#80/#81), 1 V4 misfile, mixed-script files, 1 pre-#82 bench orphan in default — targets for archivist prompt fixes (plan step 3). Gate order earned its keep: mypy caught a swapped unpack before tests ever ran |
| 87 | 2026-09-09 | Atom validation campaign closed (plan 1-3, corpus re-atomized). Step 3: the #80 date prompt rule stays -- partial (zawita clean, nastroyka run-dependent), 4B keeps STABLE invented dates across runs (Jul 10 2024, May 29 / Aug 22-24 2024, 27 Aug -- parametric beliefs, 14B-class). Per-part language header REVERTED: flips 4B atoms fully into English (refutes #67's header hope; language residual proven 14B-class twice). Demotion searches the quote across the whole atom (`_atom_bounds`)
: line-scope silently killed genuine decisions, caught by component B (e2e gate on a fixed synthetic chat, decision deliberately PENDING, assertion checks the verbatim acceptance quote in any observed form; green in the full gate, skipped when the LLM server is down). V2 parses the Unix date format ("Fri Aug 7 08:20:28 2026" from pasted terminal output; 13 false flags on nastroyka). Mode [2] producer is quiet per atom (owner decision): one end-of-run error total + mode [3] hint; the full audit stays mode [3]. index.progress log carries its data in the message. Corpus end state: 85 true errors (59 V2, 26 V5), zero V6 warnings after a clean index rebuild -- the x2 duplicate upsert and the pre-#82 bench orphan both cleared by rebuild; x2 root cause unfixed (indexing code untouched, future candidate) |
| 88 | 2026-09-09 | Duplicate upsert fixed (root cause from #87). IVectorStore.upsert default (add-then-delete) leaves a coexistence window: interruption inside it = permanent duplicate; mtime filter then skips the doc, old set never deleted. Both adapters override the port default with atomic replace (one lock, one rebuild, one save; RAM snapshot rollback; max_chunks + dim checks). No disk format change. Tests: re-upsert of same source must not grow the store; other sources survive |
| 89 | 2026-09-09 | Sources blocks reached the LLM via client-provided history (#51) — #49's strip covered storage only; the model imitated the format (double blocks, cross-topic names) and chat-path refusals carried sources (#50 never added there). Fix: strip ALL matching blocks; history sanitized at the ChatManager choke point; refusal = no sources on both chat paths. Rule: every door history enters the model through gets the same cleaning |
| 90 | 2026-09-09 | Atoms producer hardened: stable invented dates (#87) entered the index because V2 is read-only by design (#86) — ungrounded date tokens in template structures (Context parens, as-of markers, Chronology) are now stripped at birth (verbatim-or-covered vs the source; prose left to V2); cross-part exact duplicates removed in code (the FINAL merge instruction was structurally unfulfillable — parts are independent requests); decision-quote check gained a format-agnostic fallback (whole-source verbatim search when no user-block markers, #80/#81; attribution rests on the DECISION TEST). Producer contract: three birth-time correctors (dedup, decision demotion, date guard), each printing a run total |
| 91 | 2026-09-10 | V5 compared atoms to the FILE's own script: a whole-file language flip was invisible (chasov atoms cyr=0 from an RU chat) and the surviving correct atoms got flagged instead (routera: 8 RU in an EN-flipped file). Fix: reference = the SOURCE chat's script (URLs stripped; no letter threshold — quiet fallback hides flips; fallback only when no source). Rule: a language check needs an external reference — self-comparison cannot see a uniform flip. Also: split_file strips markdown image noise on every path (owner decision; old parts not retro-processed) |
| 92 | 2026-09-10 | Atoms A/B on the live corpus (owner, 3 runs): raw+split+noise-strip beats or ties 4B atoms on coverage, trap questions and personal-experience queries; atoms lost brand lists and user experience, and the language ceiling flipped 5/6 files EN. Policy: atoms OFF until a 14B-class model fits VRAM (Ceiling Log trigger); raw is the full layer; Golden Set is the enforcement. Guards (date/decision/dedup/V5) stay in prepare_docs for the 14B day. Rule: a distillation layer must measurably beat its source before it ships — on 4B it does not. Same-day refinement: not folder-wide OFF — per-document via the raw_documents_atom basket (explicit copy = intent; drift #92 enforcement); atomized docs live beside raw in one index, mixed serving verified |
| 93 | 2026-09-10 | Watcher skip trusted uri+mtime only; a partially-cleared store (external clear, interrupted write) was invisible — half the chunks gone, all docs "skipped" (#82 lost 103 chunks this way). Reproduced live: half-cut store → "Skipped 7"; touch → full self-repair via upsert; full clear → 7/7 exact rebuild. Fix: skip only when mtime matches AND stored chunk count == total_chunks; unknown totals fail open (re-index, idempotent). Rule: a skip condition must verify the artifact it vouches for, not just the key it remembers |
| 94 | 2026-09-10 | #81 closed as not-reproducible: two Golden runs (2026-09-10, new llama.cpp build, raw-only corpus) — zero false refusals on RU-direct queries across ~14 questions; the 'shassi' flake proven generation-side (3 observed shapes, all honest). Recorded cause stays unattributed; if it resurfaces, capture trace_id + rerank candidates before touching anything |
| 95 | 2026-09-11 | clean_cache live-log guard was pgrep-based — silently off on Windows (drift #74 rule). Port check via socket (mirrors run_servers PORTS) — one mechanism, all OSes; tasklist cannot see uvicorn (runs as python.exe) |
| 96 | 2026-09-11 | run_scripts.py venv relaunch used Unix-only os.execl — on Windows the console can close on double-click (run_servers.py has the Windows-safe pattern since inception). Same subprocess.call branch applied; Unix path byte-identical. Windows branch untested live — verified when back on Windows |
| 97 | 2026-09-11 | Required-V5 e2e assertion red ×4 stable (7B, post-09-10 engine): atoms now RU from the RU chat — the engine update cured the EN flip the assertion locked as mandatory. Test imports none of the audit edits (run_servers/clean_cache/run_scripts); earlier green runs had the server down (skip). Fix: drop the required-defect assert, keep non-V5-must-be-empty; V5 stays unit-covered. Lesson: never lock a known defect as a required outcome — engine updates cure defects and turn the lock red; re-baseline e2e gates together with check_rag after engine updates |
| 98 | 2026-09-11 | e2e quote-survival flake on 7B post-09-10-engine: acceptance paraphrased instead of quoted ~1/4 runs (3G/1R). No code change: the assert is the #87 gate; xfail/flaky marking either adds a dependency or blinds the gate (§13.4 — never accommodate an observed failure). multi-turn-1 precedent applied: single red = re-run, two consecutive = investigate; revisit at 14B or next engine update |
| 99 | 2026-09-11 | download_tokenizers wrote tokenizer.json directly — a network cut mid-download left a truncated file; startup is fatal on corrupt tokenizer files (drift #73), so a failed download bricked the app until manual deletion. Atomic write (tmp + rename), same pattern as core/io_utils. Existing valid files untouched |

## FUTURE RISKS (18)
| Risk | Trigger | When to fix |
|------|---------|-------------|
| Split date-grounding criteria: producer (_ground_dates) accepts verbatim-or-covered, V2 checks covered-only — a date present verbatim in the source but yielding no parsed covering token gives a false V2 error | First observed false V2 flag | Unify into one _is_grounded shared by producer and validator |
| make_atoms is all-or-nothing: no retry, no per-part checkpoint — a transient HTTP failure mid-run discards the accumulated answers (zero errors measured, #65) | First network failure or lost long run | Bounded retry (3 attempts, single layer); per-part checkpoint only if losses repeat |
| Atom self-sufficiency is a prompt declaration, not a checked property: atoms with unresolved references ("he/it", "see above") or degenerate length pass V1-V6 | Unresolved-reference atoms observed in the live corpus | Add V7 as warn-only heuristics after the owner confirms real cases |
| _split_for_atoms cuts hard seams: a fact spanning two parts is extracted per-part (no proven loss to date; overlap would feed the duplication the #90 dedup guards) | A documented fact lost or misattributed specifically at a part seam | Overlap, dedup-aware; no change without a proven seam defect |
| The archivist prompt FINAL instruction still promises cross-part merge / Chronology completeness that independent per-part requests cannot deliver (#90 closed only the mechanical dedup half) | Next planned archivist prompt revision | Drop the unfulfillable sentence or feed prior-parts digests into FINAL (token-budget aware) |
| Atoms output is free-form markdown: a recurring malformed-format class would require regex surgery over the corpus | Malformed atoms become a documented recurring failure class | JSON-mode output — rewrites prompt, validators and corpus; only for a proven syntactic (not semantic) defect class |
| Config migration bloat | >10 migrations in config.py | When loading old configs becomes slow |
| Test fatigue | Skipping tests due to mock boilerplate | When test coverage drops below 80% |
| LLM prompt coupling | Prompt instability on 14B+ models | When upgrading to 14B+ |
| Vector store RAM limit | Hitting 100K chunks | When max_chunks reached |
| Async lock complexity | Deadlocks in production | When deadlock rate > 1/month |
| Sibling-source deletion | Watcher reindexes one of 2+ sources sharing a namespace; orphan cleanup sees only that source's URIs and deletes the siblings' chunks | Before mapping a second source to a namespace |
| Phantom empty namespaces | `list_by_filter` on a never-populated namespace creates it in memory; the memory adapter later lists it and persists an empty store file | When namespace lifecycle code is touched |
| Silent fallback prompt | Invalid prompt_name (API request or config) renders `prompts/v1/fallback.j2` with degraded RAG instructions; only a log entry, no error to the caller | When prompt versioning/registry work begins |
| Hybrid search (BM25+vector, RRF) | Real corpus shows missed exact-term retrieval (BM25-zero vs vector hits on user queries) | CORE CHANGE: new index format + port extension + index sync design |
| RAM headroom shrink | Peak RAM (6.5/15 GB at ~140 bench docs) grows past ~10 GB with real corpus | Before adding any RAM-heavy component; measure first |
| 7B bench multi-turn-1 ('Why?' followup): FLAKY, nondeterministic — 4 red / 2 green incl. one green on cold (2026-09-08 08:33; cold-warm correlation disproven); §14 greedy jitter amplified by chat condensation flips the verdict — first counterexample to 'all verdict-neutral'; [000]-correlated ([d] stable historically), live [d]+'Почему?' green; step unattributed (no condensed-query logging); 4B unaffected | flake rate grows, or numeric prefix used live | log condensed query (§2.7); rule: re-run before classifying a surprising 7B red |
| atoms-undated-1 red on both models (4B evades 'when'-answers 'what'; 7B confabulates by merging facts); prompt-level cure leaks into anti-echo rules (drift #85) | 14B-class model adopted, or a generation-side date-phrase guard designed | re-attempt date honesty at that layer |
